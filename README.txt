al iniciar entra en la carpeta principal en la terminal y para iniciar el docker es el comando
" docker compose up -d --build " 
para entrar al menu del endpoint --->  http://localhost:8000/doc#/
archivos.csv
los archivos se crean automaticamente al ejecutarse el docker
